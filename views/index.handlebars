<h1>Burgers!!!</h1>


<div class="row">
<div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">

<h2>Burgers, waiting to be devoured.</h2>

<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
      <li>
        {{this.burger_name}} <span> | </span> {{this.createdAt}}

        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="true">
          <button type="submit">Devour the Burger!</button>
        </form>
      </li>
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
<p><img src="assets/img/Cheeseburger.png"></img></p>
</div>

<div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
<h2>DEVOURED BURGERS!!!!!</h2>

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      <li>
        {{this.burger_name}} <span> | </span> {{this.createdAt}}

        {{!-- <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="false">
          <button type="submit">This Burger was devoured!!!  Make another one?</button>
        </form> --}}
      </li>
    {{/if}}
  {{/each}}
</ul>
</div>
</div>

<h2>Build-a-Burger</h2>
<form class="create-update-form" action="/" method="POST">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="slee">Devoured?</label><br>
    <input type="radio" name="devoured" value="1"> Yes!!<br>
    <input type="radio" name="devoured" value="0" checked> Not yet..
  </div>

  <button type="submit">Add Burger</button>
</form>